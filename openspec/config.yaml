schema: spec-driven
spec-gen:
  version: 1.0.0
  generatedAt: 2026-04-05T08:50:47.894Z
  domains:
    - analyzer
    - api
    - services
    - generator
    - drift
    - verifier
    - types
    - cli
    - spec
    - openspec
    - import
    - chat
    - mcp-handlers
    - utilities
    - app
  confidence: 0.95
context: >-
  # User-provided context (preserved)

  # User-provided context (preserved)

  # Auto-detected by spec-gen

  Tech stack: TypeScript, React, Vitest

  Architecture: Layered (routes -> controllers -> services -> repositories)

  Domains: analyzer, api, services, generator, drift, verifier, types, cli, viewer, spec, openspec,
  chat, stages, mcp-handlers, utilities, commands, components, app

  Key patterns: Component-based UI


  # Auto-detected by spec-gen

  Tech stack: TypeScript, React, Vitest

  Architecture: Layered (routes → controllers → services → repositories)

  Domains: analyzer, generator, drift, verifier, chat, mcp-handlers, cli, api, viewer, spec,
  services, types

  Key patterns: Component-based UI


  # Auto-detected by spec-gen

  Tech stack: TypeScript, React, Vitest

  Architecture: Layered (routes → controllers → services → repositories)

  Domains: analyzer, api, services, generator, drift, verifier, types, cli, viewer, spec, openspec,
  import, chat, stages, mcp-handlers, utilities, eslint-config, commands, components, app

  Key patterns: Component-based UI
